option_settings:
  aws:elasticbeanstalk:application:environment:
    STREAMLIT_SERVER_PORT: "8080"
  aws:elasticbeanstalk:environment:proxy:
    ProxyServer: none
  aws:elasticbeanstalk:command:
    Timeout: 1800  # 30 minutes timeout for heavy startup